# WorkBuddy 项目交接：选十

## 先读这里

这是一个专业摄影展共同选片工具，不是通用投票后台。

第一版只服务一个真实流程：

1. 杨可若上传某个体育馆约 100 张已初筛、调色完成的照片。
2. 邀请玄总及其他选片人。
3. 每人独立给每张照片打 1 至 5 星，可写一句个人备注。
4. 提交前不能看到他人的评分和备注。
5. 全部提交后，按平均分综合排序。
6. 再用独立的“终选”标记，严格选出 10 张进入展览。

## 产品原则

按优先级排序：

1. **照片最大**：审片时，照片必须尽可能占满屏幕。
2. **操作最快**：方向键切图，数字键 1 至 5 评分并进入下一张。
3. **完整优先**：默认适应屏幕，不能裁切；需要时切换 1:1 原图细看。
4. **界面退后**：评分、备注、文件名和导航不能抢照片的视觉注意力。
5. **独立评分**：提交前严格隐藏其他人的结果。
6. **终选独立**：五星评分和“终选”不是同一概念。
7. **极简文案**：能删除的说明全部删除，只留完成任务必须的信息。

用户对“全屏”的定义是浏览器 Fullscreen API 的真实全屏，不是网页内部的 fixed 遮罩。Fullscreen API 在 Perplexity 内嵌预览 iframe 中不可用，必须在独立顶层网页测试。

## 当前产品边界

必须保留：

- 动态添加选片人
- 1 至 5 星独立评分
- 每张照片的一句话个人备注
- 提交前隐藏他人评分
- 全部提交后的平均分排序
- 独立“终选”标记
- 严格限制恰好 10 张
- 完成后复制文件名
- 全屏、1:1、前后切图和键盘评分

不要加入：

- AI
- 评论讨论串
- 复杂账号系统
- 多轮评审
- 多维评分
- 策展排版
- 公开投稿
- 授权管理
- 自动替人决定结果
- 儿童身份信息

如果新需求超出上述边界，先说明它会扩大第一版范围，不要直接实现。

## 当前实现

这是无构建步骤的静态前端原型：

- `index.html`：页面骨架、顶部流程和全屏查看器
- `styles.css`：完整视觉系统、响应式和全屏查看样式
- `main.js`：状态、页面渲染、评分、备注、终选和全屏交互
- `assets/`：17 张演示照片
- `README.md`：产品概述

没有框架，没有 npm 依赖，没有后端。不要为了“工程化”擅自改成 React、Vue 或引入组件库。

当前状态只保存在内存中，刷新页面会重置。用户上传的本地照片通过 Object URL 临时显示。多人协作、永久保存和云端照片尚未实现。

## 本地运行

不要直接双击 `index.html`。在仓库根目录运行：

```bash
python3 -m http.server 4173
```

然后打开：

```text
http://localhost:4173
```

真实全屏必须在顶层浏览器标签页测试，不能在 IDE 内嵌预览、iframe 或 Perplexity 预览里判断。

## 当前线上环境

- GitHub：`https://github.com/edyang816/keruo-photo-select`
- Render 测试站：`https://keruo-photo-select.onrender.com`
- 默认分支：`main`
- Render 当前只作临时静态托管
- `select.keruoyang.com` 尚未连接

Render 发布目录是仓库根目录，入口脚本必须保持为 `main.js`。此前使用 `app.js` 时线上返回 404，导致空白页。

## 设计方向

创建和任务管理页面：

- 暖白背景
- 黑灰文字
- 单一橙红强调色
- 少量细线
- 不使用卡片堆叠、渐变、阴影和大段说明

审片页面：

- 纯深色背景
- 单张照片是绝对主角
- 默认完整适应视口
- 导航覆盖在照片边缘，不预留大块布局空间
- 评分和备注压缩在底部
- 任何新增控件都必须说明为什么值得占用照片空间

不要做成常见 SaaS Dashboard，也不要为了“高级感”加入无意义动画。

## 下一步优先级

### P0：把审片体验做准

建议将评分入口调整为“单张全屏审片优先，网格作为总览”，不要让用户先面对 100 张小图再逐张点击。

需要重点试验：

- 进入评分后是否直接打开第一张全屏图
- 单击图片切换适应屏幕与 1:1 是否足够直观
- 1:1 后的平移体验
- 横图、竖图和不同显示器比例
- 评分后自动下一张的节奏
- 备注输入是否打断连续评分
- 手机端是否保留评分效率

### P1：稳定页面结构

- 创建任务
- 任务等待页
- 独立评分
- 综合排序与终选
- 最终 10 张

在这一步结束前，不接数据库。

### P2：再接真实协作

视觉和交互稳定后，再实现：

- 云端照片上传
- 任务持久化
- 邀请链接
- 评委身份
- 独立评分隔离
- 评分与备注保存
- 最终结果同步

## 基础设施方向

长期以腾讯云为主要基础设施，并面向全球访问。原型阶段可以使用 Render，但不要写入 Render 专属业务逻辑。

保持以下边界：

- 前端保持可静态部署
- 照片存储接口与 UI 分离
- 数据 API 与前端状态分离
- 环境变量不写进仓库
- 不把照片 URL、用户身份或任务数据绑定到单一供应商格式

未来预期可以迁移为：

- 静态前端：腾讯云 COS + CDN
- 原图与预览图：腾讯云 COS
- API：腾讯云云函数或容器服务
- 数据库：腾讯云数据库

具体服务选型等真实协作阶段再决定，不要现在提前搭建。

## 修改前检查

先回答：

1. 这个修改是否让照片更大或审片更快？
2. 是否增加了第一版不需要的功能？
3. 是否在真实顶层浏览器中测试？
4. 是否同时测试横图、竖图、桌面和手机？
5. 是否仍然严格限制恰好 10 张终选？

## 最低验收

每次改动至少验证：

- 创建任务后能进入每位选片人的评分入口
- 点击照片进入浏览器真实全屏
- 默认完整显示，不裁切
- 1:1 模式有双向滚动空间
- 左右键切图
- 数字键 1 至 5 评分并自动下一张
- 备注不会丢失
- 未评完不能提交
- 提交后不能再修改
- 所有人提交前不能进入综合终选
- 第 11 张不能被标记为终选
- 恰好 10 张时可以确认
- 手机端没有横向溢出

## 工作方式

- 小步修改，不做无关重构
- 每个视觉判断都在浏览器截图中验证
- 提交信息写清改了什么
- 推送 `main` 后检查 Render 是否真的部署了最新 commit
- 不只看 Render 显示 `live`，还要检查 `index.html`、`main.js`、`styles.css` 和至少一张图片均返回 200

